let quantity = 0;
const display = document.querySelector(".line");
const addButton = document.querySelector(".Add-to-wishlist");

// let const and var are used for variables

// if (addButton && display) {
//   addButton.onclick = function () {
//     quantity += 1;
//     console.log(`The quantity is now: ${quantity}`);
//     display.innerText = `There are only ${quantity} in your cart`;
//   };
//   display.innerText = `There are only ${quantity} in your cart`;
// }
